# Agent instructions

@CLAUDE.md

This workspace runs the ProtoFramework operational protocol. No other hints
file is used, CLAUDE.md stays the single source of truth.

**Operational rules for agent sessions:**
- Run from this directory — hints load only from the session's working directory.
- The owner is the human; decisions go in proto/decisions.md (DEC-###).
